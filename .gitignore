# Dependencies
vendor/
node_modules/

# Build artifacts
*.zip
*.mo

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Runtime artifacts (never commit local WP data)
*.log

# Contract snapshot scratch output (sync tool workspace artifact)
/backend-snap.json

# A stray copy of the shell theme once synced here; the real theme lives at wp-content/themes/
themes/
